name: Keep Supabase awake
on:
  schedule:
    - cron: '17 9 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping database
        run: |
          curl --fail --silent --show-error \
            "https://yboclyvstedyznkkcvrx.supabase.co/rest/v1/listings?select=id&limit=1" \
            -H "apikey: sb_publishable__fOVibsHCfaxepG3q6ekYg_lwimZhYM"
